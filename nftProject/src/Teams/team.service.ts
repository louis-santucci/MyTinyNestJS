import { Injectable } from '@nestjs/common';


@Injectable()
export class TeamService {
    createTeam(name: string, balance: number): void {
        return;
    }
    
    addMember(id: number): void {
        return;
    }

    updateBalance(balance: number): void {
        return;
    }
}